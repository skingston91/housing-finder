# Copy to .env.local for Vite (VITE_* only exposed to client — keep secrets server-side).
# Lambda env vars are set in template.yaml or AWS console / Parameter Store — not VITE_*.

# Optional: public map tile URL template (if using raster tiles with attribution in UI)
# VITE_MAP_ATTRIBUTION=
