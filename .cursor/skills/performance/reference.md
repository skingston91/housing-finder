# Performance Skill — Reference

This skill is derived from the project’s **performance agent rules** under `.agentrules/typescript/generic/performance/`.

## Rule Files

| File | Content |
|------|---------|
| `index.md` | Overview, priority framework (CRITICAL → LOW), quick reference snippets, monitoring. |
| `react.md` | React-only: async waterfalls, React.memo, useMemo, useCallback, lazy state, bundle/code splitting, context, virtualization, dependency arrays. |
| `javascript.md` | JS-only: Promise.all / conditional async, loop optimization, Set/Map lookups, object/array ops, memory (listeners, cache, closures), strings, debounce/throttle. |

## Project audits

Add housing-finder–specific audit docs under `docs/` (e.g. long area lists, map tiles) as the app grows.
