# `api/` (legacy path)

HTTP handlers now live under **[`lambda/`](../lambda/)** and deploy with **AWS SAM** ([`template.yaml`](../template.yaml)).

See [docs/infrastructure/aws-sam.md](../docs/infrastructure/aws-sam.md).
